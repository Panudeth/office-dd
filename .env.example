# คีย์ใส่ได้ 2 ทาง เลือกทางไหนก็ได้
#
#  1) ใส่ในหน้าเว็บ — กดปุ่ม 🔑 บนแถบบน (คีย์เก็บใน localStorage ของเบราว์เซอร์เท่านั้น)
#     เหมาะกับให้คนอื่นมาลองเล่นด้วยคีย์ของเขาเอง
#
#  2) ใส่ในไฟล์นี้ — คัดลอกเป็น .env.local แล้วเติมคีย์ (คีย์ในหน้าเว็บจะถูกใช้ก่อนเสมอ)
#
# ไม่ใส่เลยก็รันได้ — ระบบจะเข้าโหมดสาธิต (animation ครบ แต่คำตอบเป็นข้อความตัวอย่าง)

# --- Claude (Anthropic) --- ต้องมีเครดิตในบัญชี (subscription Max/Pro ใช้กับ API ไม่ได้)
ANTHROPIC_API_KEY=
# CLAUDE_MODEL=claude-opus-5

# --- Gemini (Google) --- มี free tier
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.7-flash

# ถ้าใส่ทั้งสองคีย์ ระบุว่าจะใช้อันไหน: anthropic | gemini
# ไม่ระบุ = ใช้อันที่มีคีย์ (Claude มาก่อน)
# LLM_PROVIDER=gemini


# ============================================================
# Supabase — สำหรับ Sign In + Create My Office + จ้างแล้วไม่หาย
# ไม่ใส่ก็รันได้ แอปจะทำงานแบบในเครื่องอย่างเดียว (รีเฟรชแล้วพนักงานหาย)
#
# วิธีเปิดใช้:
#   1. สร้างโปรเจกต์ที่ supabase.com
#   2. เอา supabase/schema.sql ไปรันใน SQL Editor
#   3. คัดค่าสองอันล่างจาก Project Settings → API
#   4. รีสตาร์ท dev server
#
# คีย์ฝั่ง client ใส่ได้ทั้งชื่อใหม่และชื่อเก่า ใครมีอันไหนก็ใช้อันนั้น
#   ชื่อใหม่: PUBLISHABLE_KEY = sb_publishable_...   (Supabase ใช้ชื่อนี้แล้ว)
#   ชื่อเก่า: ANON_KEY        = JWT ยาว ๆ
# ทั้งคู่ปลอดภัยที่จะให้เบราว์เซอร์เห็น เพราะ RLS ใน Postgres ยังบังคับอยู่
#
# ห้ามเอา secret key (sb_secret_...) หรือ service_role มาใส่เด็ดขาด
# สองอันนั้นข้าม RLS ได้ทั้งหมด แอปจะเตือนให้ถ้าเผลอใส่
# ============================================================
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=


# ============================================================
# ฝั่งเซิร์ฟเวอร์ (ไม่ขึ้นต้นด้วย NEXT_PUBLIC_ = เบราว์เซอร์ไม่เห็น)
# ทั้งหมดนี้ "ไม่ใส่ก็รันได้" - แอปจะทำงานแบบเบราว์เซอร์ล้วนเหมือนเดิม
# ============================================================

# Secret key ของ Supabase (Project Settings -> API -> Secret keys: sb_secret_...) หรือ service_role เก่า
# เปิดใช้: บันทึกการประชุมแบบสดลง DB (ทุกจอเห็นเหมือนกัน), token ออฟฟิศ, MCP, LINE, API
# ห้ามใส่ในตัวแปร NEXT_PUBLIC_ เด็ดขาด
SUPABASE_SECRET_KEY=

# คีย์ LLM "สำรอง" สำหรับงานที่ไม่มีเบราว์เซอร์ส่งคีย์มา (MCP / LINE / API)
# ปกติไม่ต้องตั้ง: เจ้าของ/exec ใส่คีย์ในหน้าเว็บ ("คีย์ของฉัน") แล้วระบบ sync ขึ้น DB (เข้ารหัส) ให้ MCP/LINE/API ใช้ตามรายคน
# ค่าตรงนี้ใช้เฉพาะออฟฟิศที่ยังไม่เคย sync - เลือกอย่างใดอย่างหนึ่ง
# LLM_KEY_SECRET=            (ไม่บังคับ - กุญแจเข้ารหัสคีย์ใน DB, ไม่ตั้งจะถอดจาก SUPABASE_SECRET_KEY; เปลี่ยนแล้วคีย์เก่าถอดไม่ได้ ต้องบันทึกใหม่จากหน้าเว็บ)
# Ollama บนเครื่องเดียวกับเซิร์ฟเวอร์: ไม่ต้องมีคีย์ แค่ชี้ base URL
# LLM_PROVIDER=openai
# OPENAI_BASE_URL=http://localhost:11434/v1
# OPENAI_MODEL=qwen3:8b      (ไม่ใส่ = ใช้ตัวแรกที่ ollama list มี / ใส่ชื่อที่ไม่มีในเครื่อง = ถอยไปใช้ตัวแรกแล้วเตือนใน log)
# OPENAI_API_KEY=            (เจ้าบนเน็ตเช่น Groq ต้องใส่)
# สลับไป Gemini/Claude: ตั้ง LLM_PROVIDER=gemini|anthropic + คีย์ของเจ้านั้น - ค่า OPENAI_* จะถูกข้ามทั้งหมด ไม่ต้องลบ

# LINE Official Account -> แผนกประชาสัมพันธ์ตอบลูกค้า (ออฟฟิศเดียวต่อ deployment)
# Webhook URL ใน LINE Developers = https://<โดเมน>/api/line/webhook
# LINE_CHANNEL_SECRET=
# LINE_CHANNEL_ACCESS_TOKEN=
# LINE_OFFICE_ID=            (uuid ออฟฟิศ - ดูในหน้าออฟฟิศ ส่วน "การเชื่อมต่อภายนอก")
# LINE_DEPT=pr
