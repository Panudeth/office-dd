node_modules/
.next/
out/
.env
.env.local
.env*.local
next-env.d.ts
*.tsbuildinfo
.DS_Store

# token ออฟฟิศสำหรับ MCP (สร้างจากปุ่มเชื่อมต่อ) - ห้าม commit
.mcp.json

# dist ของ dev server ทดสอบ (NEXT_DIST_DIR)
.next-test/

# log ของ task รายวันบิล GCP
.gcp-billing.log

# ไฟล์ของ Claude Code (ตั้งค่าส่วนตัว/เครื่องใคร เครื่องมัน) - ไม่เอาขึ้น repo
.claude/
CLAUDE.md
CLAUDE.local.md
.claude*
